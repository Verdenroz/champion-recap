{
	"name": "fetch-matches-lambda",
	"version": "1.0.0",
	"description": "Lambda function to fetch matches from Riot API",
	"main": "index.js",
	"scripts": {
		"build": "tsc",
		"package": "npm run build && zip -r function.zip ."
	},
	"dependencies": {
		"@aws-sdk/client-dynamodb": "^3.700.0",
		"@aws-sdk/client-lambda": "^3.700.0",
		"@aws-sdk/client-s3": "^3.700.0",
		"@aws-sdk/client-sqs": "^3.700.0",
		"@aws-sdk/lib-dynamodb": "^3.700.0"
	},
	"devDependencies": {
		"@types/node": "^22.0.0",
		"typescript": "^5.9.2"
	}
}
